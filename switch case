Write a program that reads input from user and return age in days/hours  using switch statement.

using namespace std;
void main () { 
	int age,choice;

	cout<<("Enter your age");
	cin>>age;
	cout<<("Enter 1 for age in Days , Enter 0 For age in hours");
	cin>>choice;
	switch(choice) {
	case 1:
		age=age*365;
		cout<<(" Your age in days is:")<<age<<endl;
		break;
	case 0:
		age=age*365*24;
		cout<<("your age in hours is:")<<age<<endl;
		break;
	default:
		cout<<("invalid input");
	}
	system("pause");
}
